<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= blog.title %> - My Blogs
  </title>
  <link rel="stylesheet" href="styles/layout.css">
  <link rel="stylesheet" href="styles/content.css">
</head>

<body>
  <%- include('partials/header.ejs') %>

    <div class="blog-detail-container">
      <h1 class="blog-title">
        <%= blog.title %>
      </h1>
      <h3 class="blog-subject">
        <%= blog.subject %>
      </h3>
      <h4 class="author-name">By: <%= blog.author %>
      </h4>

      <% if (blog.image) { %>
        <img class="blog-image" src="public/Images/<%= blog.image %>" alt="<%= blog.title %>">
        <% } %>

          <div class="blog-body">
            <p>
              <%= blog.body %>
            </p>
          </div>
    </div>

    <%- include("partials/footer.ejs") %>
</body>

</html>